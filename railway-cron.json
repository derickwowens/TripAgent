{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.cron"
  },
  "deploy": {
    "startCommand": "npx tsx data/scripts/cron-sync.ts",
    "restartPolicyType": "NEVER",
    "cronSchedule": "0 6 * * 1"
  }
}
