FROM node:20-slim

WORKDIR /app

# Copy package files and install dependencies
COPY package*.json ./
RUN npm ci --omit=dev

# Copy data scripts and tsconfig
COPY data/ data/
COPY tsconfig.json ./

# Run the cron sync script
CMD ["npx", "tsx", "data/scripts/cron-sync.ts"]
